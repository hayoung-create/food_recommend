# NutriPick 작업 목록 (TASKS)

기준: `docs/PRD.md`, `docs/DEVELOPMENT_PLAN.md`  
규칙: 한 번에 하나의 체크 항목만 구현하고, 완료 시 `[x]`로 표시한다.

제외됨 (이미 완료): 프로젝트 생성, 가상환경, FastAPI/React scaffold, CORS, `/health`

---

## MVP

### A. 백엔드 — 데이터 수집 & DB

- [x] A-1. `.env` / `.env.example`에 `DATA_GO_KR_SERVICE_KEY` 키 설정 안내 추가 (코드 하드코딩 금지, `.gitignore`에 `.env` 확인)
- [x] A-2. `Product` SQLModel 정의 (식품명, 분류, 제조사, 칼로리·탄수화물·단백질·지방·당류·나트륨·포화지방·콜레스테롤)
- [x] A-3. 공공 API 1페이지 샘플 호출로 응답 필드명 매핑표 확정 (제조사 필드 포함)
- [x] A-4. 페이지네이션 수집 스크립트 작성 (`backend/scripts/`)
- [x] A-5. 수집 실패 시 최대 3회 재시도 · 실패 페이지 스킵 · 로그
- [x] A-6. 수집 데이터를 SQLite(`app.db`)에 저장 (upsert)
- [x] A-7. 전처리: 100g/100ml 기준·결측(NULL) 처리 점검 (결측 제품 제외하지 않음)
- [x] A-8. 적재 건수·식품분류 목록이 DB에서 조회되는지 확인

### B. 백엔드 — 추천 알고리즘

- [x] B-1. Min-Max 정규화 함수 구현
- [x] B-2. 낮을수록 가점 항목 반전 로직 (칼로리, 지방, 당류, 나트륨)
- [x] B-3. 목표별 가중치 상수 정의 (다이어트 / 고단백 / 저염식 / 저당식)
- [x] B-4. 가중합 → `recommend_score = round(score * 100)` 계산
- [x] B-5. 동점 시 단백질 높은 순 정렬
- [x] B-6. 전체 식품 기준 TOP5 추출
- [x] B-7. 카테고리/중분류 지정 시 해당 범위 제품만 점수 정렬
- [x] B-8. 분류 제품 수 5개 미만일 때 `lowSampleWarning` 플래그
- [x] B-9. 결측 영양 항목은 점수 0, 응답에는 null/"정보 없음"용 값 유지
- [x] B-10. 추천 이유 문구 생성 (조건 없으면 기본 문구)

### C. 백엔드 — API

- [x] C-1. `GET /api/goals` — 건강 목표 목록
- [x] C-2. `GET /api/categories` — 대분류·중분류 계층 목록
- [x] C-3. `GET /api/recommendations` — goal 필수, category/category2 옵션, 페이지네이션
- [x] C-4. `GET /api/products/{id}` — 상세 영양 + 점수 + 추천 이유 (+ 차트용 분류 평균)
- [x] C-5. `GET /api/search` — 식품명 부분 일치 (공백·대소문자 무시)
- [x] C-6. 검색/추천 결과 없음·잘못된 goal 등 에러·빈 배열 응답 정리
- [x] C-7. 런타임 경로에서 공공 API를 호출하지 않음을 확인 (SQLite만 사용)

### D. 프론트엔드 — 공통

- [x] D-1. 페이지 라우팅 추가 (`/`, `/recommend`, `/products/:id`, `/search`)
- [x] D-2. `api.js`에 goals / categories / recommendations / product / search 호출 추가
- [x] D-3. 공통 Header (로고, 검색, 뒤로가기/홈)
- [x] D-4. 공통 Button, Badge, Spinner, EmptyState, ErrorState
- [x] D-5. 디자인 토큰(색·여백·카드) 반영 — `DESIGM_SYSTEM.md` / `UI_GUIDE.md` 기준
- [x] D-6. 필요 라이브러리만 추가 (라우터, 아이콘, Chart.js, 스타일 등)

### E. 프론트엔드 — 홈

- [x] E-1. Hero 섹션 (`docs/reference/main.png` 참고)
- [x] E-2. 건강 목표 카드 4종 UI
- [x] E-3. 목표 선택 상태(selected) 표시
- [x] E-4. "추천받기" 클릭 시 `/recommend?goal=...` 이동

### F. 프론트엔드 — 추천 결과

- [x] F-1. 선택한 목표 배너 표시
- [x] F-2. 식품분류 2단 칩 목록 로드·선택 (대분류 → 중분류)
- [x] F-3. 분류 변경 시 추천 API 재호출 (category / category2 필터)
- [x] F-4. TOP5 제품 카드 리스트 (이름, 분류, 칼로리, 단백질, 점수)
- [x] F-5. 로딩 / 에러 / 표본 부족 안내 문구
- [x] F-6. 카드 클릭 시 상세 페이지 이동

### G. 프론트엔드 — 상세

- [x] G-1. 제품 요약 (이름, 분류, 제조사, 추천 점수)
- [x] G-2. 영양성분 표 (100g 기준, 결측은 "정보 없음")
- [x] G-3. Chart.js 막대그래프 (칼로리·단백질·지방·당류·나트륨)
- [x] G-4. 추천 이유 리스트 표시

### H. 프론트엔드 — 검색 (MVP 최소)

- [x] H-1. Header에서 검색 화면/모달 진입
- [x] H-2. 식품명 검색 API 연동
- [x] H-3. 결과 리스트 및 "검색 결과가 없습니다" Empty State
- [x] H-4. 검색 결과에서 상세 페이지 이동

### I. MVP 마무리

- [x] I-1. Desktop First + 반응형 점검 (태블릿/모바일)
- [x] I-2. 접근성: 키보드 포커스, 버튼 최소 높이 44px
- [x] I-3. README에 실행법 · `.env` 키 · 데이터 수집 방법 정리
- [x] I-4. 스모크: 목표 → 추천 → 상세 → 검색 한 사이클 통과
- [x] I-5. 추천 API 응답이 SQLite 기준 3초 이내인지 확인

---

## 추가 개발 (MVP 이후)

> MVP가 끝난 뒤, 별도 지시가 있을 때만 진행한다.

### P1 — 검색·상세 보완

- [x] P1-1. 검색창 옆 식품분류 드롭다운 필터
- [x] P1-2. 상세 차트에 카테고리 평균 비교를 명확히 고정
- [x] P1-3. 추천 점수가 카테고리 스코프 기준임을 UI에 명시 강화

### P2 — 데이터·UX 보완

- [ ] P2-1. 수동 데이터 재수집 절차/스크립트 사용법 문서화 (스케줄러 없음)
- [x] P2-2. 추천/상세 카드 썸네일·플레이스홀더를 참고 UI에 맞게 정리
- [ ] P2-3. 분류 미선택 vs 선택 시 점수 차이 안내 카피 개선

### 운영 메모 (선택)

- [ ] O-1. 전체 데이터 수집 실행 (`python -m scripts.collect_foods --all`)

### 제외 (구현하지 않음)

- 회원가입 / 로그인
- 즐겨찾기 (상세 하트 동작 포함)
- 리뷰 작성
- AI 챗봇
- OCR 영양성분표 인식
- 식단 기록 / 칼로리 계산기
- 결제 / 쇼핑몰 연동
- 의료적 진단
- 검색 자동완성

---

## 진행 메모

| 항목 | 내용 |
|------|------|
| MVP 상태 | A~I · P1 · P2-2 · 제품 비교(`/compare`) |
| 다음 작업 | (선택) P2-1 / P2-3 / O-1 |
| 가상환경 | `backend/venv` 유지 (삭제하지 않음) |
| `.env` | 유지됨 (키는 그대로) |
| 문서 우선순위 | PRD → UI_GUIDE → DESIGN_SYSTEM |
